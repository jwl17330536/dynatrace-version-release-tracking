name: Release Tracking — repo hygiene

on:
  workflow_dispatch:

jobs:
  hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository hygiene checks
        run: bash scripts/repo-hygiene-check.sh
