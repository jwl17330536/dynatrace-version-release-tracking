# Consumer References

This folder stores snapshots of downstream assets that consume Version Intelligence outputs but are owned by other repositories.

Current consumer snapshots come from `dynatrace-private-ops/dashboards/` and are provided for dependency traceability only.

These files are references and are not the canonical maintenance location for those dashboards.
